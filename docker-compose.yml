version: "1"

services:
  angular-ssr:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kodey-crm-front
    ports:
      - "4300:4300"
    environment:
      - PORT=4300
    restart: unless-stopped
